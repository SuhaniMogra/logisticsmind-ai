def generate_subqueries(main_query):
    return [
        f"{main_query} latest trends",
        f"{main_query} market analysis",
        f"{main_query} risks and opportunities",
        f"{main_query} future outlook"
    ]