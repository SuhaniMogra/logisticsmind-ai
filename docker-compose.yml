version: "3.9"

services:
  logisticsmind:
    build: .
    container_name: logisticsmind-app
    ports:
      - "8080:8000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/storage:/app/backend/storage